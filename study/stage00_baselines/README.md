# 阶段零：已有能力的基线实验

本目录保留课程期间对上游已有功能进行的Graph、重叠调度和TP对照。它们不是本项目新增优化的成绩。

- `bench_cuda_graph.py`：CUDA Graph开关，支持1或8请求。
- `bench_overlap.py`：重叠调度开关。
- `bench_tp.py`：TP=1/2，多进程离线回放。
- JSON文件保存各次测量与中位数。

激活依赖环境并设置`MODEL_PATH`后执行。原始实验使用A800与Llama-3.1-8B。TP=1/2输出哈希不同，尚未专门定位，不能直接引用阶段一的单请求差异结论解释。
