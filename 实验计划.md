
## 实验计划

建议按 **5 周、6 个阶段**推进，项目暂定为：

**基于 Mini-SGLang 的长短请求混合负载调度优化**

核心问题是：**在持续到达的长短请求混合负载下，能否减少 decode 输出停顿，同时控制 TTFT 和吞吐量的损失？**

第一版采用 prefill、decode 分批交替执行，暂不实现 mixed batching。

**实验的统一设置**

|项目|设置|
|---|---|
|硬件|单张 A800，实验前检查独占情况|
|模型|本地 Llama-3.1-8B-Instruct|
|基线|当前版本原始 prefill 优先策略，记录 Git commit|
|初期配置|开启 CUDA Graph，关闭重叠调度，使用 naive cache|
|后期验证|开启重叠调度；再测试 Radix cache|
|输出控制|性能实验固定生成长度、忽略 EOS；正确性测试单独覆盖 EOS|
|重复方式|每配置预热；至少 5 次独立测量，保留原始数据|
|请求发送|按预先生成的时间表发请求，不因前一个请求未完成而推迟后续请求|
|公平比较|同一场景各策略使用相同输入、到达时间和输出上限|

先关闭重叠调度，是为了简化状态追踪；最终优化必须在开启它时重新验证。

**阶段 1：建立测量工具——第 1 周**

目标：能够回答“请求在哪里等待、GPU 每轮在处理什么”。

记录两类数据：

| 请求级记录                | 调度轮次记录              |
| -------------------- | ------------------- |
| 到达、首次调度、首 token、完成时间 | 本轮 prefill 或 decode |
| 每个输出 token 的时间       | 请求数、新处理 token 数     |
| 输入与实际输出长度、结束原因       | 等待队列长度、KV 使用情况      |

分别标明客户端可见时间与引擎内部时间，不把文本流中的一个网络块直接当成一个 token。

输出指标：

- TTFT、ITL、端到端延迟的 P50/P95/P99。
- 输出 token 吞吐量、成功请求数、错误数。
- 最长排队时间、最长 decode 等待时间。

**交付物：**请求回放脚本、JSONL 原始记录、统计脚本、时间线图。  
**验收：**请求数量和输出数量可核对，时间戳顺序正确，记录功能的额外开销经过测量。常规计时不在每轮插入全局 CUDA 同步。

**阶段 2：复现长 prefill 干扰——第 2 周前半**

先做一个容易解释的实验：

```
让若干短请求进入 decode
          ↓
注入一批长提示词请求
          ↓
观察短请求的输出间隔是否出现尖峰
```

建议的起始工作负载：

|类型|输入长度|输出长度|
|---|---|---|
|短请求|128 token|256 token|
|长请求|4096 token|128 token|

这些是实验起点，不是最终参数。需要先确认最大序列长度和 KV 容量足够，避免正常调度实验被显存不足混淆。

比较 prefill 预算 `256、1024、4096`，观察：

- 分块变小是否减少单次停顿？
- 原始 prefill 优先策略是否仍让 decode 连续等待？
- 长请求 TTFT、总吞吐量如何变化？

**交付物：**基线复现报告，以及“prefill 轮次—decode 输出间隔”对照图。  
**验收：**现象能重复出现，并能对应到实际调度记录；如果没有明显干扰，先调整负载、分析原因，不急着改策略。

已按阶段整理，并完成阶段二实验：

```
study/
├── stage00_baselines/             # 已有功能基线
├── stage01_measurement/           # 测量工具与差异调查
└── stage02_prefill_interference/  # 长 prefill 干扰实验
```

共完成 **30 次正式测量**。长请求突发后，短请求最大输出间隔的五次中位数：

|Prefill 预算|最大输出间隔|停顿中的连续 prefill 批次|
|---|---|---|
|256|1.781 秒|64|
|1024|1.375 秒|16|
|4096|1.255 秒|4|

**结论：仅缩小分块不能让 decode 获得执行机会。** 原始策略仍连续执行 prefill，小分块在本次实验中反而增加了累计停顿。尚未修改核心调度代码。

6 项测试通过，所有请求按指定长度完成，GPU 已释放。预算 256 的突发组存在跨运行输出差异，已保存，原因尚未单独验证。

- [中文实验报告](study/stage02_prefill_interference/REPORT.md)
- [停顿放大时间线](study/stage02_prefill_interference/evidence/gap_timeline.svg)

已推送分支 `feat/stage2-prefill-interference`，最新提交 **`80e502b`**，尚未合并到 `main`。



**阶段 3：实现公平调度策略——第 2 周后半至第 3 周**

按顺序实现并比较：

| 策略            | 用途          |
| ------------- | ----------- |
| 原始 prefill 优先 | 基线          |
| 简单 decode 优先  | 对照，展示另一端的权衡 |
| 带等待时间约束的策略    | 你的主要改动      |

你的策略第一版只使用少量规则：

1. decode 等待超过阈值时，安排 decode。
2. prefill 等待过久时，安排 prefill。
3. 两者都触发时，使用明确、固定的决策规则。
4. 保留 prefill token 预算。

时间阈值是调度触发条件，不能承诺严格延迟上限，因为已执行的 GPU 工作不能随意抢占。

**交付物：**可切换的策略配置、调度决策日志、必要的状态与边界测试。  
**验收：**持续有 prefill 或 decode 时，另一类可执行请求仍能获得机会；输出和资源状态正确。



**阶段 4：系统评估——第 4 周**

先固定分块预算比较策略，再针对有价值的策略扫预算，避免一次组合太多变量。

|场景|目的|
|---|---|
|纯短请求|检查常见场景的额外开销|
|纯长请求|检查 prefill 性能退化|
|长短混合、稳定到达|比较常态延迟与吞吐量|
|长请求突发|检查输出停顿和尾延迟|
|持续 decode 加新请求|检查 prefill 饥饿|
|KV 容量紧张|检查资源约束下的行为|

每个主要场景测试低负载、中负载、接近饱和三档。到达率由先导实验确定，并在策略间保持一致。参数调节与最终评估使用不同的随机种子和到达序列。

**主指标：**混合负载下的 P95/P99 ITL。  
**约束指标：**TTFT、吞吐量、错误率和最长等待时间。

可以预先设定“吞吐量下降不超过 5%”作为候选验收门槛，但它是项目目标，不是承诺。若达不到，报告实际权衡，不隐藏失败场景。

**交付物：**延迟—吞吐量对照图、不同负载下的结果表、适用边界说明。  
**验收：**报告每次测量的波动；P99 数据量不足时明确标注，增加请求数后再下结论。

**阶段 5：正确性与性能归因——第 5 周前半**

正确性覆盖：

- EOS、长度上限、请求取消。
- 分块请求继续执行和结束。
- 前缀共享、缓存淘汰、资源回收。
- 开启重叠调度后的结束处理。
- 长时间运行后无请求丢失、重复返回或资源泄漏。

选择一个代表性混合负载，用 Nsight Systems 比较修改前后的时间线。Profiler 运行与正式计时分开。

**交付物：**测试结果和带说明的 GPU 时间线。  
**验收：**能用源码与时间线解释收益来源；输出差异若出现，需要调查，不能直接归因于浮点误差。

**阶段 6：整理成面试项目——第 5 周后半**

最终仓库应包含：

- 小而清晰的调度器改动。
- 一键复现实验的脚本与配置。
- 原始数据、分析脚本和图表。
- 正确性测试。
- 一份报告：问题、方案、结果、代价、局限。
- 对原始项目功能与个人贡献的明确区分。

自适应分块作为**后续扩展**：只有固定策略已验证有效，才根据测得的执行时间调整预算，避免第一版范围过大。

**我们的第一个里程碑是阶段 1 和阶段 2：不改调度策略，先做出可信的测量工具，并复现长 prefill 导致 decode 停顿。** 这两步完成后，再根据数据确定修改方案。

